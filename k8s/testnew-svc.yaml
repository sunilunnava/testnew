{
    "kind": "Service",
    "apiVersion": "v1",
    "metadata": {
        "name": "testnew",
         "namespace": "com-att-ocnp-mgmt"
    },
    "spec": {
        "selector": {
            "app": "testnew"
        },
        "ports": [
            {
                "protocol": "TCP",
                "port": 80,
                "targetPort": 8080
            }
        ],
        "type": "NodePort"
    }
}
